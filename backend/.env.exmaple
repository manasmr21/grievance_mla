# Application
PORT=8080

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_NAME=grievance_mla

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d

NODE_ENV=development


mail=
password=


COOKIE_SECRETE = "sic mundus creatus est"

FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:8080

LOCATION_JSON_URL=http://13.200.126.131/india_state_list.json

# Mail campaign — Gmail rate limits (~30 emails/min safe)
MAIL_CAMPAIGN_SEND_DELAY_MS=2000
MAIL_CAMPAIGN_BATCH_DELAY_MS=5000
MAIL_CAMPAIGN_SMTP_RETRIES=3
MAIL_CAMPAIGN_SMTP_RETRY_DELAY_MS=10000